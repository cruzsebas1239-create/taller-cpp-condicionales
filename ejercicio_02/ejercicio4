#include <iostream>
#include <cmath>   

using namespace std;

int main() {
    double op1, op2;
    char operador;

    cout << "Ingrese el primer número: ";
    cin >> op1;

    cout << "Ingrese el segundo número: ";
    cin >> op2;

    cout << "Ingrese el operador (+, -, *, /, %): ";
    cin >> operador;

    switch (operador) {
        case '+':
            cout << "Resultado: " << (op1 + op2) << endl;
            break;
        case '-':
            cout << "Resultado: " << (op1 - op2) << endl;
            break;
        case '*':
            cout << "Resultado: " << (op1 * op2) << endl;
            break;
        case '/':
            if (op2 == 0) {
                cout << "Error: División por cero no permitida." << endl;
            } else {
                cout << "Resultado: " << (op1 / op2) << endl;
            }
            break;
        case '%':
            if (op2 == 0) {
                cout << "Error: Módulo por cero no permitido." << endl;
            } else {
                cout << "Resultado: " << fmod(op1, op2) << endl;
            }
            break;
        default:
            cout << "Error: Operador inválido." << endl;
            break;
    }

    return 0;
}
